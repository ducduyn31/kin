# Kin Environment Configuration
#
# Copy to:
#   .env.local      - Local development
#   .env.staging    - Staging builds
#   .env.production - Production builds
#
# Usage:
#   task run                  # uses .env.local
#   task build:apk:staging    # uses .env.staging
#   task build:apk:prod       # uses .env.production

# Environment name (development, staging, production)
ENVIRONMENT=development

# Auth0 Configuration
# Get these from your Auth0 Dashboard > Applications > Your App
AUTH0_DOMAIN=your-tenant.auth0.com
AUTH0_CLIENT_ID=your_client_id_here
AUTH0_AUDIENCE=

# App scheme for OAuth callbacks (your bundle ID)
# iOS: Must match CFBundleURLSchemes in Info.plist
# Android: Must match scheme in AndroidManifest.xml intent-filter
APP_SCHEME=com.example.kin

# API Configuration
API_BASE_URL=

# Feature Flags
ENABLE_DEBUG_LOGGING=true
